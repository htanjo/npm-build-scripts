{
  "private": true,
  "scripts": {
    "start": "npm run serve",
    "serve": "browser-sync start --server public --no-notify --files public/",
    "build": "imagemin public/ public/"
  },
  "devDependencies": {
    "browser-sync": "^2.11.0",
    "imagemin-cli": "^2.1.0"
  }
}
